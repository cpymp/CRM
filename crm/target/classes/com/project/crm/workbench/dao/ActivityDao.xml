<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.crm.workbench.dao.ActivityDao">
        <select id="getTotalByCondition" resultType="int">
        SELECT COUNT(*) FROM
             tbl_activity ac
            JOIN tbl_user user
            ON   ac.owner = user.id
            <where>
                <if test="name != null and  name!=''">
                    ac.name like '%' #{name} '%'
                </if>
                <if test="owner != null and owner !=''">
                    and  user.name like '%' #{owner} '%'
                </if>
                <if test="startDate != null and startDate != ''">
                    and ac.startDate &gt; #{startDate}
                </if>
                <if test="endDate != null and endDate != ''">
                    and ac.endDate  &lt; #{endDate}
                </if>
            </where>
        </select>
        <select id="getActivityByCondition" resultType="Activity">
            SELECT
                ac.id,
                ac.name,
                user.name AS owner,
                ac.startDate,
                ac.endDate
            FROM tbl_activity ac
            JOIN tbl_user user
            ON   ac.owner = user.id

            <where>
                <if test="name != null and  name!=''">
                        ac.name like '%' #{name} '%'
                </if>
                <if test="owner != null and owner !=''">
                       and  user.name like '%' #{owner} '%'
                </if>
                <if test="startDate != null and startDate != ''">
                        and ac.startDate &gt; #{startDate}
                </if>
                <if test="endDate != null and endDate != ''">
                     and ac.endDate  &lt; #{endDate}
                </if>
            </where>

            ORDER BY ac.createTime DESC
            limit #{skipCount},#{pageSize};
        </select>
<!--   DESC  降序    ASC 升序 -->









	<insert id="save"  >

    insert  into tbl_activity(
            id,
            owner,
            name,
            startDate,
            endDate,
            cost,
            description,
            createTime,
            createBy
    )
            values (
            #{id},
            #{owner},
            #{name},
            #{startDate},
            #{endDate},
            #{cost},
            #{description},
            #{createTime},
            #{createBy}
                   )

    </insert>
	
</mapper>
























